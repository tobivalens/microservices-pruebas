apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: backend
data:
  JWT_SECRET: "PRFT"
  REDIS_HOST: "redis.data.svc.cluster.local"
  REDIS_PORT: "6379"
  REDIS_CHANNEL: "log_channel"
  AUTH_API_PORT: "8000"
  TODO_API_PORT: "8082"
  SERVER_PORT: "8083"
  PORT: "8080"

---
# ConfigMap en el namespace frontend
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: frontend
data:
  PORT: "8080"
 
